namespace UnitTests;

public class Tests
{
    
}

/**
using HTML_NET;
using HTML_NET.Parser;

var byteBuffer = new ByteBuffer(File.ReadAllBytes(@"E:\projects\HTMLParser\Tests\html_basic_document.html"));
var tokenizer = new HTMLTokenizer(byteBuffer);

var token = tokenizer.NextToken();
while (token != null)
{
    Console.WriteLine($"Type: {token.Type}");
    token = tokenizer.NextToken();
}*/